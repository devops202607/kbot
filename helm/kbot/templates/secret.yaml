{{- if .Values.secret.create }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "kbot.fullname" . }}-secret
  labels:
    {{- include "kbot.labels" . | nindent 4 }}
type: Opaque
stringData:
  TELE_TOKEN: {{ .Values.secret.teleToken | quote }}
{{- end }}
